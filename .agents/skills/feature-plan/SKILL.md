---
name: feature-plan
description: Save an existing reviewed feature plan without changing it.
---

Read the [canonical workflow](../../../.claude/commands/feature-plan.md) in
full. Follow it.

Invoke as `$feature-plan NN`. Replace each `$1` in the canonical workflow
with `NN`. Its allowed tools and one-file rule are binding.
